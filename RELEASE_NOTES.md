# cr3-2026.09.11

本版本位于独立分支 `versions/cr3-2026.09.11`，基于原 `main` 提交 `c640c9b947681266b2a64ce6cafd440bb4e9fa51`。原分支及历史版本不覆盖、不合并。可在 GitHub 选择本分支后用 Code → Download ZIP 下载完整源代码。

## 改进

- 预检仅复用完全相同的成功 IK 查询，保留 User/Tool 和关节分支约束，新增进度、耗时与缓存命中统计；预览复用网格数据，并可通过 `--skip-previews` 省去绘图。
- 使用真实机器人反馈决定接触后压入位置；反馈丢失或无效时停止，不再用指令目标冒充实测 TCP。
- 高度测量增加已探明区间内的 0.02 mm 细步搜索、释放验证和原位多帧确认；新增数据导出、平面校正拟合及独立验证工具。
- 加入实际抬高横梁 STL 和几何检查结果：中央支撑点为 `(0, -139, 27)`，旧托座名义 Z 为 12。几何预览配置不能用作真实机器人标定。
- 150 mm 入口改用抬高横梁配置路径，避免隐式选择旧 v1 托座。

## 验证与适用范围

120 项离线单元及集成测试通过；2000 点离线计划和 HTML 可视化已生成并检查。测试不连接机械臂或相机，尚未测量现场 IK 提速。

0.1 mm 是观测误差预算的验收上限，尚未认证真实绝对精度。仍需匹配实物的设计配置、真实 fixture profile 和独立测量证据。当前平面参考点仅覆盖 R01 局部区域，不能据此校正整板；程序拒绝外推和非零倾角。

详见 [安装、离线预览和实测校正流程](README_CR3_IMPROVED.md)。本版本不包含现场位姿、触觉采样日志或运行环境。
